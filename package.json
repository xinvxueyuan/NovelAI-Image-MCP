{
  "name": "novelai-image-mcp-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Node-side tooling for the NovelAI Image MCP repository (markdownlint only).",
  "license": "MIT",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "markdownlint": "markdownlint-cli2",
    "markdownlint:fix": "markdownlint-cli2 --fix"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.18.1"
  }
}
